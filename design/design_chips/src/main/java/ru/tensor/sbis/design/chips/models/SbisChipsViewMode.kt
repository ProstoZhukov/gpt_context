package ru.tensor.sbis.design.chips.models

/**
 * Режим отображения фона элементов.
 *
 * @author ps.smirnyh
 */
enum class SbisChipsViewMode {

    /** Отображение фона невыбранных элементов. */
    FILLED,

    /** Фон отображается только у выбранных элементов. */
    GHOST
}