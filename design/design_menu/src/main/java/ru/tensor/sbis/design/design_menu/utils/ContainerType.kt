package ru.tensor.sbis.design.design_menu.utils

/**
 * Тип контейнера в котором отображается компонент меню.
 *
 * @author ra.geraskin
 */
internal enum class ContainerType {

    /**
     * В контейнере (в окне).
     */
    CONTAINER,

    /**
     * В шторке (MovablePanel).
     */
    PANEL;
}