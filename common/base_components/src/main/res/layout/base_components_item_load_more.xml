<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="ru.tensor.sbis.base_components.adapter.vmadapter.LoadMoreVM"/>
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="?offset_s"
        android:paddingTop="?offset_s">

        <ru.tensor.sbis.design.progress.SbisLoadingIndicator
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:indeterminate="@{viewModel.indeterminate}"
            app:SbisLoadingIndicator_progressSize="m"/>

    </FrameLayout>
</layout>