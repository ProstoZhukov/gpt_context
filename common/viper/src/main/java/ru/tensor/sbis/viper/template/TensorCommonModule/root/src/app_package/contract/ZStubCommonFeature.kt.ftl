package ${packageName}.contract

import android.content.Context

/**
 * Предоставление внешней функциональности для других модулей
 *
 * @param context контекст текущего состояния приложения
 */
class ${moduleName}CommonFeature(private val context: Context)