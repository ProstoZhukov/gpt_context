package ${packageName}.contract.internal

import ${commonNamespace}.crud.${moduleName}.${modelName}CommandWrapper

internal interface ${modelName}Interactor {

        val commandWrapper: ${modelName}CommandWrapper
}