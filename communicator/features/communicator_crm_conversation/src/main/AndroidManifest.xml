<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Доступ в интернет для контроллера -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Доступ на регистрацию и получение сообщений от Google Cloud Messaging -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>

    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>

    <application android:allowBackup="false">

        <activity
            android:name="ru.tensor.sbis.communicator.crm.conversation.CRMConversationActivity"
            android:theme="@style/AppTheme.Swipe.Back"
            android:windowSoftInputMode="adjustPan"
            android:exported="true">

            <intent-filter android:label="@string/communicator_conversation_activity_title">
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

            <intent-filter>
                <action android:name="${applicationId}.CRM_CONVERSATION_ACTIVITY"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

        </activity>

    </application>

</manifest>